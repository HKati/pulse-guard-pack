# Base settings for link checking
exclude = [
  "^mailto:",
  "^tel:",
  "^#",
  "localhost",
  "127.0.0.1",

  # badges / dinamikus képek, privát repón gyakran 404 vagy 302 → lychee fail
  "^https://img\\.shields\\.io/.*",
  "^https://github\\.com/.*/actions/workflows/.*badge\\.svg$"
]

max-concurrency = 8
timeout = 20
github = true

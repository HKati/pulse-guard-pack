name: Guard (PR)

on:
  pull_request:

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  guard:
    uses: HKati/pulse-guard-pack/.github/workflows/secret-leak-guard.yml@v1
    # The reusable guard already handles defaults internally.
    # No need to pass undefined inputs (fail-on-find, scan-history, extra_args).

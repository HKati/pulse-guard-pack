name: Guard self-test

on:
  workflow_dispatch:
  pull_request:
    branches: [ main ]

jobs:
  guard:
    uses: HKati/pulse-guard-pack/.github/workflows/secret-leak-guard.yml@main
    with:
      fail-on-find: true

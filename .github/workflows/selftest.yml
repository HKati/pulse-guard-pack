name: Guard self-test

on:
  pull_request:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  guard:
    uses: eplabsai/pulse-guard-pack/.github/workflows/secret_guard.yml@v1
    # optional: pass custom args, config
    # with:
    #   gitleaks_version: "v8.18.2"
    #   extra_args: "--config-path .gitleaks.toml"
